'use strict';

const SensorDriver = require('../sensor/driver');

module.exports = class extends SensorDriver {
/*
From the API the sensor-19040 is exactly the same as the original Smart sensor.
Many users see this as a different device so this device has different assets.
*/
};
